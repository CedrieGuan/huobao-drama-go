# 实施计划

> 本文档是 Frontend → Swift 原生化迁移的执行路线图，定义了每个阶段的具体任务、验收标准、Agent 分工和依赖关系。

> 产品前提已确认：本计划交付的是 **macOS 专用客户端**，不是浏览器端 Web 替换。Nuxt 前端用于提炼功能与交互基线；Swift App 通过可配置的 backend base URL 连接现有后端。

---

## 1. 总体路线图

```
Week 1 ─── Phase 1: 基础设施 (Agent A + B 并行)
Week 2 ─── Phase 2: 核心页面 (Agent C + D + E 并行)
Week 3-4 ─ Phase 3: 工作台深化 (Agent F + G + H 并行)
Week 5 ─── Phase 4: 联调打磨 + 原生增强
```

**总工期：5 周**

---

## 2. Phase 1 — 基础设施（第 1 周）

### 2.1 Agent A：基础框架 + 数据模型 + API 层

#### Day 1-2：项目骨架 + Models

**任务清单**:

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| A1 | 创建 Xcode 项目 | `HuobaoDrama.xcodeproj` | — |
| A2 | App 入口 + 导航框架 | `App/HuobaoDramaApp.swift` | ~30 |
| A3 | 路由定义 | `App/Router.swift` | ~40 |
| A4 | API 响应包装 | `Models/APIResponse.swift` | ~15 |
| A5 | 枚举定义 | `Models/Enums.swift` | ~180 |
| A6 | Drama 模型 | `Models/Drama.swift` | ~30 |
| A7 | Episode 模型 | `Models/Episode.swift` | ~35 |
| A8 | Character 模型 | `Models/Character.swift` | ~35 |
| A9 | Scene 模型 | `Models/Scene.swift` | ~20 |
| A10 | Storyboard 模型 | `Models/Storyboard.swift` | ~80 |
| A11 | AIServiceConfig 模型 | `Models/AIServiceConfig.swift` | ~50 |
| A12 | AgentConfig 模型 | `Models/AgentConfig.swift` | ~25 |
| A13 | Skill 模型 | `Models/Skill.swift` | ~15 |
| A14 | VoiceProfile 模型 | `Models/VoiceProfile.swift` | ~15 |
| A15 | 请求/响应辅助类型 | `Models/RequestTypes.swift` | ~250 |

**验收标准**:
- [ ] Xcode 项目可编译运行，显示空白窗口
- [ ] 所有 Model 可通过 `JSONDecoder` 正确解析后端返回数据
- [ ] CodingKeys 正确映射 snake_case ↔ camelCase
- [ ] 编写 Model 解析单元测试（至少覆盖 Drama、Episode、Storyboard、AIServiceConfig）

#### Day 3-4：API 层

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| A16 | APIClient 核心实现 | `Services/APIClient.swift` | ~80 |
| A17 | 全部 API 端点定义 | `Services/APIEndpoints.swift` | ~300 |
| A18 | 连接配置存储 | `Services/ConnectionSettingsStore.swift` | ~40 |
| A19 | 轮询协调器 | `Services/PollingCoordinator.swift` | ~80 |
| A20 | 下载导出服务 | `Services/DownloadExportService.swift` | ~60 |

**验收标准**:
- [ ] `APIClient` 可成功调用后端 `GET /api/v1/dramas` 并解析返回
- [ ] 56 个 API 端点函数全部定义且类型正确
- [ ] App 可读取并持久化 backend base URL
- [ ] 轮询协调器可驱动图片/视频/合成状态刷新
- [ ] 下载导出服务可将后端静态资源保存到本地

#### Day 5：联调 + 文档

| # | 任务 | 说明 |
|---|------|------|
| A21 | 与后端联调 | 启动后端 `npm run dev`，验证全部 CRUD 端点 |
| A22 | API 层单元测试 | `Tests/APIClientTests.swift` |
| A23 | 导出接口文档 | 确保其他 Agent 可直接调用 `APIEndpoints.*` |

**交付物**:
```
HuobaoDrama/
├── HuobaoDrama.xcodeproj
├── HuobaoDrama/
│   ├── App/
│   │   ├── HuobaoDramaApp.swift
│   │   └── Router.swift
│   ├── Models/              (10 个文件)
│   └── Services/            (5 个文件)
└── HuobaoDramaTests/
    └── Services/
        ├── APIClientTests.swift
        └── ModelDecodingTests.swift
```

---

### 2.2 Agent B：设计系统 + 公共组件

#### Day 1-2：设计系统

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| B1 | 色彩系统 | `DesignSystem/Colors.swift` | ~80 |
| B2 | 字体系统 | `DesignSystem/Typography.swift` | ~25 |
| B3 | 间距 + 圆角 + Token | `DesignSystem/DesignTokens.swift` | ~40 |
| B4 | 阴影系统 | `DesignSystem/Shadows.swift` | ~30 |
| B5 | ViewModifier 扩展 | `DesignSystem/ViewModifiers.swift` | ~60 |

**色彩映射验收**:
- [ ] `Color.bg0` ≈ `#FFFFFF`（对应 `--bg-0`）
- [ ] `Color.accent` ≈ `#4C7DFF`（对应 `--accent`）
- [ ] `Color.text0` ≈ `#182132`（对应 `--text-0`）
- [ ] 状态色 success/error/info/warning 全部定义

#### Day 3-5：公共组件

| # | 任务 | 产出文件 | 行数估算 | 对应前端 |
|---|------|----------|----------|----------|
| B6 | 搜索下拉选择器 | `Views/Common/SearchablePicker.swift` | ~200 | `BaseSelect.vue` |
| B7 | 卡片容器 | `Views/Common/CardView.swift` | ~30 | CSS `.card` |
| B8 | 标签 | `Views/Common/TagView.swift` | ~40 | CSS `.tag` |
| B9 | 状态圆点 | `Views/Common/StatusDot.swift` | ~20 | CSS `.dot` |
| B10 | 加载覆盖层 | `Views/Common/LoadingOverlay.swift` | ~25 | CSS `.step-loading` |
| B11 | 空状态 | `Views/Common/EmptyStateView.swift` | ~40 | CSS `.step-empty` |
| B12 | 图片全屏预览 | `Views/Common/ImageViewer.swift` | ~60 | `imageViewer overlay` |
| B13 | 音频播放器 | `Views/Common/AudioPlayerView.swift` | ~50 | `<audio controls>` |
| B14 | 视频播放器 | `Views/Common/VideoPlayerView.swift` | ~50 | `<video controls>` |
| B15 | 步骤导航条 | `Views/Common/StepBubbleBar.swift` | ~80 | `step-bubble` |
| B16 | 模态弹窗修饰符 | `Views/Common/ModalSheet.swift` | ~30 | `.overlay .modal` |

**SearchablePicker 验收标准**:
- [ ] 支持搜索过滤选项
- [ ] 支持键盘导航（↑↓ + Enter + Escape）
- [ ] 支持占位文本
- [ ] 支持选中项高亮
- [ ] 支持传入 `[Option]` 数组，Option 包含 `label` / `value`
- [ ] 下拉面板不超出窗口边界

**交付物**:
```
HuobaoDrama/DesignSystem/          (5 个文件)
HuobaoDrama/Views/Common/          (11 个文件)
```

---

## 3. Phase 2 — 核心页面（第 2 周）

> **前置条件**: Phase 1 的 Agent A 和 Agent B 全部交付，代码已合并到主分支。

### 3.1 Agent C：项目列表 + 剧集详情

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| C1 | DramaListViewModel | `ViewModels/DramaListViewModel.swift` | ~80 |
| C2 | 项目列表页 | `Views/DramaList/DramaListView.swift` | ~180 |
| C3 | DramaDetailViewModel | `ViewModels/DramaDetailViewModel.swift` | ~100 |
| C4 | 剧集详情页 | `Views/DramaDetail/DramaDetailView.swift` | ~150 |
| C5 | 添加新集弹窗 | `Views/DramaDetail/AddEpisodeSheet.swift` | ~120 |

#### DramaListView 功能验收

| 功能 | 验收标准 |
|------|----------|
| 项目网格 | `LazyVGrid(columns: [GridItem(.adaptive(minimum: 280))])` 显示项目卡片 |
| 卡片内容 | 标题、集数徽章、风格标签、角色数、场景数、进度条、更新时间 |
| 骨架屏 | 加载时显示灰色渐变动画占位 |
| 空状态 | 无项目时显示引导卡片 |
| 新建项目 | 点击弹出 Sheet，输入标题 *必填 + 集数 + 风格 |
| 删除项目 | 弹出确认对话框后删除 |
| 点击导航 | 点击卡片 `router.navigate(to: .dramaDetail)` |
| 进度计算 | 根据 episodes 中有 scriptContent 的比例计算 |
| 时间格式化 | 刚刚 / X分钟前 / X小时前 / X天前 / 日期 |

#### DramaDetailView 功能验收

| 功能 | 验收标准 |
|------|----------|
| 返回按钮 | `router.pop()` |
| 项目信息 | 标题、风格标签、角色数、场景数 |
| 剧集列表 | 纵向列表，E01/E02 编号，剧本状态，时长 |
| 添加新集 | Sheet 弹窗，标题输入 + 三选配置（图片/视频/音频） |
| 配置锁定 | 创建时必须选择三个配置，创建后不可更改 |
| 点击导航 | 点击剧集 `router.navigate(to: .studio)` |

---

### 3.2 Agent D：设置页

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| D1 | SettingsViewModel | `ViewModels/SettingsViewModel.swift` | ~200 |
| D2 | 设置页主框架 | `Views/Settings/SettingsView.swift` | ~80 |
| D3 | AI 服务配置区 | `Views/Settings/AIServiceConfigSection.swift` | ~200 |
| D4 | 配置编辑弹窗 | `Views/Settings/AIServiceConfigSheet.swift` | ~200 |
| D5 | 火宝预设弹窗 | `Views/Settings/HuobaoPresetSheet.swift` | ~120 |
| D6 | Agent 配置区 | `Views/Settings/AgentConfigSection.swift` | ~150 |
| D7 | Skills 管理区 | `Views/Settings/SkillsSection.swift` | ~150 |
| D8 | Skill 编辑弹窗 | `Views/Settings/SkillEditSheet.swift` | ~80 |

#### 功能验收

**AI 服务配置**:

| 功能 | 验收标准 |
|------|----------|
| 火宝推荐面板 | 显示 4 张预设卡片（文本/图片/视频/音频），点击弹出 API Key 输入框 |
| 快捷模板 | 4 种服务类型按钮，点击打开对应类型的新建表单 |
| 服务列表 | 按类型分组展示，每行显示 provider/name/model/base_url |
| 启用/禁用 | Toggle 开关切换 `is_active` |
| 测试按钮 | 调用 `/ai-configs/test`，展示可达/不可达结果 |
| 编辑弹窗 | 模板快捷选择 + name/provider/priority/apiKey/baseUrl/model 字段 |
| 端点前缀提示 | 根据 provider 自动显示 `baseUrl + prefix` |
| 删除 | 二次确认后删除 |

**Agent 配置**:

| 功能 | 验收标准 |
|------|----------|
| 5 个 Agent 卡片 | script_rewriter / extractor / storyboard_breaker / voice_assigner / grid_prompt_generator |
| 折叠展开 | 点击卡片展开编辑区 |
| 模型选择 | SearchablePicker 从已启用的 text 配置中获取选项 |
| 参数编辑 | Temperature (0~2) + Max Tokens (100~32000) |
| 提示词编辑 | TextEditor 编辑 system_prompt |
| 恢复默认 | 重置 system_prompt 为默认值 |
| 保存 | 调用 createOrUpdate API |

**Skills 管理**:

| 功能 | 验收标准 |
|------|----------|
| Agent 列表 | 左侧 5 个 Agent 切换 |
| Skill 列表 | 右侧显示当前 Agent 的 Skills |
| 新增 Skill | 输入 id * + name + description |
| 编辑内容 | TextEditor 编辑 Markdown 内容 |
| 删除 | 点击删除按钮 |

---

### 3.3 Agent E：工作台框架 + 剧本面板

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| E1 | StudioViewModel | `ViewModels/StudioViewModel.swift` | ~200 |
| E2 | 工作台主视图 | `Views/Studio/StudioView.swift` | ~100 |
| E3 | 顶栏 | `Views/Studio/StudioTopbar.swift` | ~80 |
| E4 | 流水线侧边栏 | `Views/Studio/PipelineSidebar.swift` | ~150 |
| E5 | 剧本面板容器 | `Views/Studio/ScriptPanel.swift` | ~40 |
| E6 | 步骤01 原始内容 | `Views/Studio/RawContentStep.swift` | ~60 |
| E7 | 步骤02 AI改写 | `Views/Studio/ScriptRewriteStep.swift` | ~80 |
| E8 | 步骤03 提取角色场景 | `Views/Studio/ExtractStep.swift` | ~120 |
| E9 | 步骤04 分配音色 | `Views/Studio/VoiceAssignStep.swift` | ~150 |
| E10 | 步骤05 分镜列表 | `Views/Studio/StoryboardStep.swift` | ~200 |
| E11 | 分镜详情编辑 | `Views/Studio/StoryboardDetailPanel.swift` | ~250 |
| E12 | 步骤导航条 | `Views/Studio/StepNavigationView.swift` | ~60 |

#### 功能验收

**StudioView 布局**:

| 功能 | 验收标准 |
|------|----------|
| HSplitView | 左侧 PipelineSidebar (~220pt) + 右侧 Main Content |
| 顶栏 | 返回按钮 + 项目标题 + 集号 + 进度 + 刷新 + 主操作按钮 |
| 主操作按钮 | 根据 mergeUrl/sbs.length 显示"查看成片"/"继续制作"/"开始制作" |

**PipelineSidebar**:

| 功能 | 验收标准 |
|------|----------|
| 三段分区 | 剧本(5步) / 制作(6标签) / 导出(1步) |
| 步骤项 | 编号 + 图标 + 标签 + 完成状态勾 |
| 进度条 | 底部固定，`pipelineProgress/11` |
| 点击跳转 | 点击任意步骤/标签切换右侧面板 |

**RawContentStep**:

| 功能 | 验收标准 |
|------|----------|
| TextEditor | 粘贴原始内容 |
| 字数统计 | 去空格字符数实时显示 |
| 保存按钮 | 调用 `episodeAPI.update(id, { content })` |

**ScriptRewriteStep**:

| 功能 | 验收标准 |
|------|----------|
| 空状态 | "AI 改写为格式化剧本" + 开始改写按钮 + 跳过按钮 |
| 加载态 | Spinner + "正在改写剧本..." |
| 结果编辑 | TextEditor 显示改写后内容，可手动修改 |
| 重新改写 | 调用 `script_rewriter` Agent |
| 跳过改写 | 直接使用 rawContent 作为 scriptContent |

**ExtractStep**:

| 功能 | 验收标准 |
|------|----------|
| 空状态 | "从剧本提取角色与场景" + 开始提取按钮 |
| 加载态 | Spinner + "正在提取角色和场景..." |
| 结果面板 | 左侧统计卡片 + 角色列表卡片 + 场景列表卡片 |
| 角色/场景卡片 | 名称 + 角色/标签 + 描述 |

**VoiceAssignStep**:

| 功能 | 验收标准 |
|------|----------|
| 空状态 | "为角色分配合适的音色" + AI 自动分配按钮 |
| 加载态 | Spinner + "正在分配音色..." |
| 音色库面板 | 左侧展示所有可用音色（label/gender/traits/suitable） |
| 角色卡片网格 | 每个角色：名称 + 描述 + 音色选择器 + 试听按钮 + 播放器 |
| 音色选择 | SearchablePicker 选择 voice_style |
| 生成试听 | 调用 `characterAPI.voiceSample` |
| 批量试听 | 为所有已分配音色的角色生成试听 |

**StoryboardStep**:

| 功能 | 验收标准 |
|------|----------|
| 空状态 | "将剧本拆解为分镜序列" + AI 拆解按钮 + 视频模型标签 |
| 加载态 | Spinner + "正在拆解分镜并生成提示词..." |
| 左右分栏 | 左侧镜头列表 + 右侧详情面板 |
| 镜头列表 | 序号 + 景别 + 描述 + 状态点（图片/视频/对白） |
| 详情面板 | 14+ 字段编辑表单（见下方字段清单） |
| 添加镜头 | 手动添加空白分镜 |
| 删除镜头 | 确认后删除 |

**StoryboardDetailPanel 字段清单**:

| 区段 | 字段 | 控件类型 |
|------|------|----------|
| 镜头结构 | title | TextField |
| | shot_type | TextField（建议列表） |
| | angle | TextField（建议列表） |
| | movement | TextField（建议列表） |
| | 角色绑定 | 多选 Pills |
| | 场景绑定 | 下拉选择 |
| | location | TextField |
| | time | TextField |
| | duration | Stepper (1~60) |
| 画面语义 | action | TextEditor |
| | result | TextEditor |
| | description | TextEditor |
| | atmosphere | TextEditor |
| | dialogue | TextEditor |
| 生成提示 | image_prompt | TextEditor |
| | video_prompt | TextEditor |
| | bgm_prompt | TextEditor |
| | sound_effect | TextEditor |
| 预览 | 首帧图片 | 异步图片 + 点击放大 |
| | 尾帧图片 | 异步图片 + 点击放大 |
| 状态标签 | 首帧/尾帧/视频 | TagView（已生成/待生成） |

---

## 4. Phase 3 — 工作台深化（第 3-4 周）

> **前置条件**: Phase 2 全部交付。Agent E 的 StudioView 框架和 ScriptPanel 可正常运行。

### 4.1 Agent F：制作面板

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| F1 | ProductionPanelViewModel | `ViewModels/ProductionPanelViewModel.swift` | ~250 |
| F2 | 制作面板容器 | `Views/Studio/ProductionPanel.swift` | ~60 |
| F3 | 角色形象子面板 | `Views/Studio/CharactersSubpanel.swift` | ~150 |
| F4 | 场景图片子面板 | `Views/Studio/ScenesSubpanel.swift` | ~120 |
| F5 | 配音子面板 | `Views/Studio/DubbingSubpanel.swift` | ~130 |
| F6 | 帧图子面板 | `Views/Studio/ShotsSubpanel.swift` | ~200 |
| F7 | 视频子面板 | `Views/Studio/VideosSubpanel.swift` | ~150 |
| F8 | 合成子面板 | `Views/Studio/ComposeSubpanel.swift` | ~150 |
| F9 | 导出面板 | `Views/Studio/ExportPanel.swift` | ~100 |

#### ProductionPanelViewModel 状态管理

```swift
// 异步任务 pending 管理
var pendingCharImageIds: Set<Int> = []
var pendingSceneImageIds: Set<Int> = []
var pendingShotFrameKeys: Set<String> = []    // "id:first_frame" / "id:last_frame"
var pendingVideoIds: Set<Int> = []
var pendingComposeIds: Set<Int> = []
var failedVideoMessages: [Int: String] = [:]
var failedComposeMessages: [Int: String] = [:]

// 轮询管理
private var pollingTask: Task<Void, Never>?

func startPolling() {
    pollingTask?.cancel()
    pollingTask = Task {
        while !Task.isCancelled {
            // 轮询 pending 项的状态
            await pollPendingItems()
            try? await Task.sleep(for: .seconds(3))
        }
    }
}
```

#### 各子面板功能验收

**CharactersSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 角色数量 + 图片配置标签 + 批量生成按钮 |
| 角色卡片网格 | 封面图 + 名称 + 角色 + 生成状态 + 生成按钮 |
| 排除旁白 | 自动过滤 name/role 含"旁白/narrator"的角色 |
| pending 态 | 生成中显示 Spinner + "生成中"文本 |
| 图片预览 | 点击图片弹出 ImageViewer |

**ScenesSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 场景数量 + 图片配置标签 + 批量生成按钮 |
| 场景卡片网格 | 封面图 + 地点 + 时间 + 生成状态 + 生成按钮 |
| 宽图模式 | 场景封面使用 16:9 比例 |

**DubbingSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 可生成数 + 已生成数/总数 + 音频配置标签 + 批量按钮 |
| 对白镜头列表 | 序号 + 说话人标签 + 对白文本 + 生成状态 |
| 音频播放器 | 已生成的 TTS 显示 AudioPlayerView |
| 空状态 | "先在分镜里填写对白" 引导 |

**ShotsSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 镜头数 + 帧图统计 + 图片配置标签 + 帧模式切换 + Grid工具入口 |
| 帧图列表 | 每行：序号 + 景别 + 描述 + 状态点 + 首帧缩略图 + (尾帧缩略图) |
| 帧模式切换 | 仅首帧 / 首尾帧 |
| 点击生成 | 点击空白缩略图触发生成 |
| Grid历史面板 | 当前宫格图预览 + 历史列表展开/收起 |
| Grid工具入口 | 按钮打开 GridToolSheet |

**VideosSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 镜头数 + 视频统计 + 批量按钮 |
| 视频卡片网格 | 视频播放器/封面图 + 描述 + 景别 + 时长 + 状态点 + 生成按钮 |
| pending 态 | 生成中显示 Spinner |
| 失败信息 | 红色错误文本显示 |

**ComposeSubpanel**:

| 功能 | 验收标准 |
|------|----------|
| 工具栏 | 镜头数 + 合成统计 + 批量按钮 |
| 合成卡片网格 | 合成视频播放器/原始视频/封面 + 状态(视频/配音/合成) + 合成按钮 |
| pending 态 | 合成中显示 Spinner |
| 失败信息 | 红色错误文本 |
| 已合成标记 | 角标显示"已合成" |

**ExportPanel**:

| 功能 | 验收标准 |
|------|----------|
| 未合成引导 | "尚未准备就绪" + 跳转制作按钮 |
| 拼接触发 | "拼接全集视频" 按钮 + 已合成数量 |
| 视频播放 | 拼接完成后显示 VideoPlayerView |
| 下载按钮 | 保存到本地（NSOpenPanel 选择位置） |
| 镜头概览列表 | 右侧列表：序号 + 描述 + 合成状态点 |

---

### 4.2 Agent G：Grid 宫格图工具

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| G1 | GridToolViewModel | `ViewModels/GridToolViewModel.swift` | ~350 |
| G2 | Grid 工具 Sheet | `Views/Studio/GridToolSheet.swift` | ~500 |

#### GridToolViewModel 完整状态

```swift
@Observable
final class GridToolViewModel {
    // 配置
    var mode: GridMode = .firstFrame
    var layout: GridLayout = .threeByThree
    var selectedShotIds: Set<Int> = []
    var singleTargetId: Int?

    // 流程控制
    var step: GridStep = .config
    var isPresented = false

    // Step 0→1: 提示词
    var isGeneratingPrompt = false
    var promptText: String?
    var promptSource: String?       // "agent" or "fallback"
    var cellPrompts: [GridCellPrompt] = []

    // Step 1→2: 生成
    var isGeneratingImage = false
    var generationStatusText: String?
    var generatedImagePath: String?
    var generatedLayout: GridLayout = .threeByThree

    // Step 2→3: 分配
    var assignments: [GridAssignment] = []
    var activeCellIndex: Int = 0
    var assignmentPage: Int = 0

    // Step 3→4: 完成
    var isSplitting = false

    // 历史
    var history: [GridHistoryItem] = []
    var showAllHistory = false

    // 存储
    var storageKey: String = ""

    // 计算
    var canStart: Bool { ... }
    var assignedCount: Int { ... }
    var assignmentPageSize: Int { ... }
    var totalAssignmentPages: Int { ... }
}
```

#### 5 步流程验收

**Step 0 — 配置**:

| 功能 | 验收标准 |
|------|----------|
| 模式选择 | 3 个模式卡片（首帧/首尾帧/多参考） |
| 布局选择 | SearchablePicker（2x2/3x3/4x4/5x5） |
| 镜头选择 | checkbox 列表（多参考用 radio） |
| 全选/取消 | 按钮切换 |
| 自动布局计算 | 根据 `selectedCount` 自动推荐布局 |
| 启动条件 | `canStart` 满足后启用"生成提示词"按钮 |

**Step 1 — 提示词预览**:

| 功能 | 验收标准 |
|------|----------|
| 提示词展示 | Scrollable 文本区域 |
| 来源标签 | "AI生成" 或 "模板兜底" TagView |
| 空白格预览 | Grid 网格展示每格的 shot_number + prompt |
| 重新生成 | 调用 `gridPrompt` API |
| 上一步 | 返回 Step 0 |
| 生成宫格图 | 进入 Step 2 |

**Step 2 — 生成中**:

| 功能 | 验收标准 |
|------|----------|
| 加载动画 | Spinner + "宫格图生成中..." |
| 状态文本 | 显示当前生成状态 |
| 自动轮询 | 每 3 秒调用 `gridStatus` 直到完成 |
| 完成自动跳转 | 成功后自动进入 Step 3 |

**Step 3 — 切分分配**:

| 功能 | 验收标准 |
|------|----------|
| 宫格图预览 | 显示生成的宫格图，叠加格子分割线 |
| 格子高亮 | 点击格子高亮对应分配行 |
| 分配表格 | 每行：格号 + 镜头选择 + 帧类型选择 + 当前绑定 |
| 分页 | assignments > 12 时分页 |
| 统计 | "X/Y 格已分配" |
| 切分按钮 | 调用 `gridSplit` API |

**Step 4 — 完成**:

| 功能 | 验收标准 |
|------|----------|
| 成功图标 | 绿色对勾 + "分配完成" |
| 统计 | "X 格已分配" |
| 关闭按钮 | 关闭 Sheet + 刷新父页面数据 |

---

### 4.3 Agent H：原生增强 + 测试

#### 任务清单

| # | 任务 | 产出文件 | 行数估算 |
|---|------|----------|----------|
| H1 | 文件导出增强 | `Services/DownloadExportService.swift` | ~100 |
| H2 | 连接诊断面板 | `Views/Settings/ConnectionDiagnosticsSection.swift` | ~60 |
| H3 | AppDelegate 集成 | `App/AppDelegate.swift` | ~80 |
| H4 | 图片缓存服务 | `Services/ImageCacheService.swift` | ~50 |
| H5 | API 测试 | `Tests/APIClientTests.swift` | ~100 |
| H6 | 连接配置测试 | `Tests/ConnectionSettingsStoreTests.swift` | ~60 |
| H7 | ViewModel 测试 | `Tests/ViewModelTests.swift` | ~80 |

#### DownloadExportService 验收

| 功能 | 验收标准 |
|------|----------|
| 图片保存 | 输入 `/static/...` 路径 → 导出到用户选择位置 |
| 音频保存 | 输入 `/static/...` 路径 → 导出到本地 |
| 视频保存 | 输入 `/static/...` 路径 → 导出到本地 |
| 文件名推断 | 默认按资源类型和剧情上下文生成建议文件名 |
| 错误处理 | 下载失败或写盘失败时给出明确提示 |

#### ConnectionDiagnosticsSection 验收

| 功能 | 验收标准 |
|------|----------|
| 当前地址显示 | 显示当前 backend base URL |
| 连通性测试 | 调用 `/api/v1/health` 并展示结果 |
| 配置校验 | 地址非法时阻止保存 |
| 重置默认 | 一键恢复 `http://127.0.0.1:5679/api/v1` |

---

## 5. Phase 4 — 联调打磨（第 5 周）

### 5.1 整体联调

| # | 任务 | 负责人 | 说明 |
|---|------|--------|------|
| I1 | 全流程打通 | 全员 | 从新建项目到导出视频的完整流程 |
| I2 | 错误处理统一 | Agent A | 全局错误提示、网络断连处理 |
| I3 | 加载态统一 | Agent B | 骨架屏/Spinner/进度条统一风格 |
| I4 | 键盘快捷键 | Agent H | 常用操作快捷键 |
| I5 | 内存泄漏检查 | Agent H | Instruments 检测 |
| I6 | 暗色模式适配 | Agent B | 验证所有页面 Dark Mode |

### 5.2 性能优化

| 优化项 | 目标 | 方案 |
|--------|------|------|
| 图片加载 | 无卡顿 | `AsyncImage` + `ImageCacheService` 缓存 |
| 长列表 | 分镜100+条流畅 | `LazyVStack` / `LazyHStack` |
| API 并发 | 并行请求 | `async let` 并行加载 characters/scenes/storyboards |
| 轮询效率 | 不阻塞主线程 | `Task` + `AsyncStream` 后台轮询 |

### 5.3 打包发布

| 任务 | 说明 |
|------|------|
| App Icon | 设计 1024x1024 应用图标 |
| 签名配置 | Developer ID 签名 |
| dmg 打包 | `create-dmg` 生成安装包 |
| Homebrew | 可选：发布到 Homebrew Cask |
| 自动更新 | 集成 Sparkle 框架 |

---

## 6. 依赖关系与并行图

```
                    ┌──────────────────────────────────┐
                    │          Phase 1 (Week 1)         │
                    │                                    │
                    │  Agent A ────── Agent B            │
                    │  (Models+API)  (Design+Components) │
                    └──────┬──────────────┬──────────────┘
                           │              │
                    ┌──────┴──────────────┴──────────────┐
                    │          Phase 2 (Week 2)         │
                    │                                    │
                    │  Agent C ─ Agent D ─ Agent E       │
                    │  (列表+详情)  (设置)  (工作台框架)   │
                    └──────┬──────────────┬──────────────┘
                           │              │
                    ┌──────┴──────────────┴──────────────┐
                    │       Phase 3 (Week 3-4)           │
                    │                                    │
                    │  Agent F ─ Agent G ─ Agent H       │
                    │  (制作面板)  (Grid)  (原生增强)      │
                    └──────────────┬─────────────────────┘
                                   │
                    ┌──────────────┴─────────────────────┐
                    │       Phase 4 (Week 5)             │
                    │                                    │
                    │        联调 + 打磨 + 发布            │
                    └────────────────────────────────────┘
```

### 阻塞关系明细

| Agent | 阻塞于 | 原因 |
|-------|--------|------|
| C | A 完成 Models + API | 需要调用 `APIEndpoints` |
| C | B 完成 Components | 需要使用 `SearchablePicker`, `CardView` 等 |
| D | A 完成 Models + API | 需要调用 AI Config / Agent Config / Skills API |
| D | B 完成 Components | 需要使用 `SearchablePicker`, `TagView` 等 |
| E | A 完成 Models + API | 需要全部 Model 和 Agent Chat API |
| E | B 完成 Components | 需要全部公共组件 |
| F | E 完成 StudioView 框架 | ProductionPanel 嵌入 StudioView |
| G | E 完成 StoryboardStep | GridToolSheet 从 ShotsSubpanel 打开 |
| H | A 完成 ConnectionSettingsStore | 连接诊断依赖统一配置入口 |

---

## 7. Git 分支策略

```
main
├── feat/swift-app-init          (Agent A: 项目初始化)
├── feat/swift-models-api        (Agent A: Models + API)
├── feat/swift-design-system     (Agent B: 设计系统)
├── feat/swift-common-components (Agent B: 公共组件)
├── feat/swift-drama-list        (Agent C)
├── feat/swift-settings          (Agent D)
├── feat/swift-studio-framework  (Agent E)
├── feat/swift-production-panel  (Agent F)
├── feat/swift-grid-tool         (Agent G)
├── feat/swift-native-enhance    (Agent H)
└── feat/swift-integration       (Phase 4 联调)
```

**合并规则**:
- Phase 内各 Agent 分支独立开发
- 每个 Phase 结束时统一合并到 `main` 并解决冲突
- 合并前必须通过该 Phase 的全部验收标准

---

## 8. 风险预案

| 风险 | 触发条件 | 预案 |
|------|----------|------|
| SwiftUI SearchablePicker 开发周期超预期 | Agent B Day 5 未完成 | 降级为原生 `Menu` + `.searchable()`，功能简化 |
| Grid 工具格子叠加交互复杂 | Agent G Week 3 末未完成 | 先实现基础分配表格，宫格图叠加预览延后 |
| 后端 API 字段与推断不一致 | 联调时 Model 解析失败 | Agent A 补充 `CodingKeys` 适配，必要时用 `snake_case` + `camelCase` 双映射 |
| 用户无法连接后端 | 本机或远端服务不可达 | 设置页提供连接诊断与 base URL 配置 |
| Phase 2 合并冲突严重 | C/D/E 同时修改公共文件 | Phase 1 严格定义接口，Phase 2 各 Agent 只改自己的目录 |

---

## 9. 里程碑检查点

| 检查点 | 时间 | 标准 | 负责人 |
|--------|------|------|--------|
| M1: 基础设施就绪 | Week 1 周五 | API 可调通，公共组件可预览 | Agent A + B |
| M2: 核心页面可运行 | Week 2 周五 | 项目列表→详情→设置 全流程可用 | Agent C + D + E |
| M3: 工作台可用 | Week 4 周五 | 11步流水线全流程可走通 | Agent E + F + G |
| M4: 发布就绪 | Week 5 周五 | dmg 可安装，全流程无崩溃 | 全员 |
